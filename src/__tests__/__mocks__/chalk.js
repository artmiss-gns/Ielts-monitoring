// Mock chalk for testing
const chalk = {
  blue: (text) => text,
  green: (text) => text,
  yellow: (text) => text,
  red: (text) => text,
  gray: (text) => text,
  grey: (text) => text,
  cyan: (text) => text,
  white: (text) => text,
  bold: (text) => text,
  dim: (text) => text
};

module.exports = chalk;